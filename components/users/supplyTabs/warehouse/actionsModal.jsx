import { useState } from 'react';

import {
    Button,
    Modal,
    ModalContent,
    ModalHeader,
    ModalBody,
    ModalFooter,
    useDisclosure,
    addToast,
    Input,
} from '@heroui/react';

export default function ActionsModal({ id }) {
    return (
        <Card>
            <CardBody>Дії</CardBody>
        </Card>
    );
}